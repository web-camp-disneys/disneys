<h1>ジャンル編集</h1>
<!-- form_with部分 -->
<%= form_with model: @genre, url: admins_genre_path, local: true do |f| %>
  <h4>ジャンル</h4>
  <%= f.text_field :name %>
  <%= f.select :is_active ,[['有効',true],['無効',false]] %>
  <%= f.submit '保存' %>
<% end %>